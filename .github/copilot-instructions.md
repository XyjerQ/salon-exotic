<!-- Workspace Copilot instructions -->
- [ ] Verify project requirements.
- [ ] Scaffolded Vite + React + Tailwind project in `salon-exotic`.
- [ ] Run `npm install` then `npm run dev` in PowerShell to start.

Replace this file with extended instructions if needed.
